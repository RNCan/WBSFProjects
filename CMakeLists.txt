cmake_minimum_required(VERSION 3.25)
project(WBSFProjects LANGUAGES CXX)

# Optionally enforce C++ standard
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

add_definitions(-DMUPARSER_STATIC)

IF(MSVC)
    # Add /EHsc to the C++ compiler flags
    # It's important to append it to existing flags, not overwrite them.
    SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} /EHsc") 
ENDIF(MSVC)

# Add -fPIC on Linux/UNIX
if(UNIX AND NOT APPLE)
    set(CMAKE_POSITION_INDEPENDENT_CODE ON)
endif()

enable_testing()

# Add subdirectories (order matters: core first)
add_subdirectory(WBSF)
add_subdirectory(WBSFModels)
add_subdirectory(WBSFTools)

# Installation rules
SET(BIOSIM_INSTALL_DIRECTORY ${CMAKE_INSTALL_PREFIX}/$<$<CONFIG:Debug>:Debug>$<$<NOT:$<CONFIG:Debug>>:Release>)
SET(BIOSIM_INSTALL_BIN_DIRECTORY ${BIOSIM_INSTALL_DIRECTORY}/bin)

# install BioSIM_API
install(FILES $<TARGET_FILE_DIR:BioSIM_API>/ DESTINATION ${BIOSIM_INSTALL_BIN_DIRECTORY})

if (WIN32)
install(FILES $<TARGET_LINKER_FILE:BioSIM_API>/ DESTINATION ${BIOSIM_INSTALL_DIRECTORY}/lib)
endif()

get_target_property(BioSIM_INCLUDES BioSIM_API INCLUDE_DIRECTORIES)
install(FILES ${BioSIM_INCLUDES}/BioSIM_API.h DESTINATION ${CMAKE_INSTALL_PREFIX}/include)
get_target_property(WBSF_SOURCE_DIR WBSFBasic SOURCE_DIR)
#message("WBSF_SOURCE_DIR : ${WBSF_SOURCE_DIR}")
install(FILES 
    ${WBSF_SOURCE_DIR}/ERMsg.h 
    DESTINATION ${CMAKE_INSTALL_PREFIX}/include/Basic)
install(FILES     
    ${WBSF_SOURCE_DIR}/ERMsg/Erexcept.h 
    ${WBSF_SOURCE_DIR}/ERMsg/Sycptref.h 
    ${WBSF_SOURCE_DIR}/ERMsg/Sycptref.hpp 
    ${WBSF_SOURCE_DIR}/ERMsg/Syenvmsg.h 
    ${WBSF_SOURCE_DIR}/ERMsg/Symessag.h 
    DESTINATION ${CMAKE_INSTALL_PREFIX}/include/Basic/ERMsg)


#install all model files
set(BIOSIM_INSTALL_BIN_MODELS_DIRECTORY ${BIOSIM_INSTALL_BIN_DIRECTORY}/Models)

#Agriculture
install(FILES $<TARGET_FILE_DIR:InsectDevelopmentDatabaseII>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:InsectDevelopmentDatabaseIII>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:MagareyInfection>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})

# Climatic
install(FILES $<TARGET_FILE_DIR:AllenWave>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:ASCE-ETc>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:ASCE-ETsz>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:BiophysicalSiteIndicesEcoloap>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:BlueStainVariables>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:CaroleCoursolle>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:CCBio>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:ChillingDegreeDay>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:ClimateMoistureIndex>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:Climatic>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:ClimaticMoistureDeficit>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:ClimaticMPB>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:ClimaticQc>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:ClimaticWind>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:Climdex>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:CornHeatUnits>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:DailyVsHourly>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:DailyVsNormals>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:DegreeDay>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:DegreeHour>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:DewPeriod>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:GrowingSeason>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:HourlyGenerator>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:PotentialEvapotranspiration>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:ReverseDegreeDay>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SiteIndexClimate>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SnowMelt>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SoilMoistureIndex>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SoilMoistureIndexQL>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SoilTemperature>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SpringFrost>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:StdPrcpETIndex>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:TminTairTmax>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:UnderbarkTemperature>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:VaporPressureDeficit>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:VHR_to_Landsat>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:WaterBalance>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:WetnessDuration>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:WinterThaw>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:WorldClimVars>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})

# Fire
install(FILES $<TARGET_FILE_DIR:FBP>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:FWI>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:FWIDroughtCode>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SummerMoisture>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})

# Other
install(FILES $<TARGET_FILE_DIR:BlueStainIndex>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:PlantHardiness>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:Solar>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})

# Phenology
install(FILES $<TARGET_FILE_DIR:ActiaInterrupta_OBL_SBW>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:ActiaInterruptaBiology>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:AprocerosLeucopoda>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:Bagworm>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:BudBurst>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:BudBurstSaintAmant>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:BudBurstSBWHost>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:BudBurstUlmus>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:EmeraldAshBorer>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:EmeraldAshBorerColdHardiness>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:EntomophagaMaimaiga>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:EuropeanElmScale>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:FallCankerworms>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:ForestTentCaterpillar>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:GypsyMothSeasonality>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:GypsyMothStability>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:HemlockLooper>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:HemlockWoollyAdelgidColdTolerance>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:HemlockWoollyAdelgidPhenology>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:JackpineBudworm>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:LaricobiusNigrinus>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:LaricobiusOsakensis>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:LeafAreaIndex>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:LeucotaraxisArgenticollis>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:LeucotaraxisPiniperda>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:LeucotaraxisSpp>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:MeteorusTrachynotus_OBL_SBW>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:MeteorusTrachynotusBiology>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:MPB-ColdTolerance>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:MPB-iModel>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:MPB-SLR>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:ObliqueBandedLeafroller>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:ShootDevelopmentIndex>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SouthernPineBeetleInfestationIndex>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SpottedLanternfly>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SpringCankerworms>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SpruceBarkBeetleLiterature>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SpruceBeetle>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SpruceBudwormBiology>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SpruceBudwormDispersal>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SpruceBudwormEldon>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SpruceBudwormLaboratory>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SpruceBudwormLysyk>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:SpruceBudwormManitoba>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:Tranosema_OBL_SBW>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:TranosemaBiology>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:WesternSpruceBudworm>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:WhitemarkedTussockMoth>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:WhitePineWeevil>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})
install(FILES $<TARGET_FILE_DIR:YellowheadedSpruceSawfly>/ DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})

# install .mdl files
install(DIRECTORY 
    WBSFInstall/Models/
    DESTINATION ${BIOSIM_INSTALL_BIN_MODELS_DIRECTORY})